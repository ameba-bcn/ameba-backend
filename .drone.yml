---
kind: pipeline
type: docker
name: default

steps:
- name: test
  image: docker:dinf
  volumes:
  - name: dockersock
    path: /var/run
  commands:
    - docker-compose build backend
    - echo `docker image ls`

services:
  - name: docker
    image: docker:dind
    privileged: true
    volumes:
      - name: dockersock
        path: /var/run

volumes:
  - name: dockersock
    temp: {}
